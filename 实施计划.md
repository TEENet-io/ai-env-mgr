# Airlock Go 双二进制 实施计划

> 版本：2026-08 · 本文说明**要做什么功能、流程怎么走、模块怎么分**。具体代码在实现阶段按此展开。
> 前置文档：`架构说明.md`（架构决策与 OSS 数据结构）

---

## 1. 总目标

用 Go 实现两个 Windows 二进制：

| 二进制 | 部署到 | 职责 |
|---|---|---|
| `agent.exe` | 每台云电脑（进镜像） | Windows 服务，定时从 OSS 拉配置 → 应用到本机 → 上报状态 |
| `admin.exe` | 管理员工作机 | 代员工登录 AI 工具、下发网站封禁策略、查看全局状态 |

**技术栈**：Go 1.22 · 阿里云 OSS SDK · `golang.org/x/sys/windows`（注册表 + 服务）· 标准库

---

## 2. 功能清单

### 2.1 agent.exe

| 功能 | 说明 |
|---|---|
| **服务自管理** | `install` / `uninstall` / `start` / `stop`：注册为开机自启的 Windows 服务 |
| **手动同步** | `sync`：立即跑一次同步，用于部署验证和排障 |
| **本机状态** | `status`：打印当前用户、封禁状态、凭据状态、AppLocker 模式 |
| **定时同步** | 服务模式下按间隔执行同步循环；间隔可由 OSS 策略远程调整（见 §5.6） |
| **唤醒即同步** | 机器从休眠/睡眠恢复后立刻同步，不等下一个周期（见 §8） |

**同步循环四步**：

1. **查绑定** → 用主机名读 `agent_workdir/_bindings/{主机名}.json`，得知服务对象（见 `OSS布局.md` §3）
2. **拉网站封禁策略** → 写入注册表（见 §5）
3. **拉 AI 工具凭据** → 投递到该员工的 profile（见 §6）
4. **上报状态** → 含本地用户清单，写回 OSS 供 admin 核对（见 §7）

### 2.2 admin.exe

| 命令 | 功能 |
|---|---|
| `login --user <名> --tool <codex\|claude\|all>` | 代员工完成 OAuth 登录，凭据直接上传 OSS |
| `user add <名> [--codex 邮箱] [--claude 邮箱]` | 加入用户名单 |
| `user disable <名>` | 停用用户 |
| `user list` | 查看名单 |
| `add-site <域名>...` | 增加封禁网站 |
| `remove-site <域名>...` | 移除封禁网站 |
| `list-sites` | 查看当前封禁清单 |
| `enable-block` / `disable-block` | 封禁总开关（临时全局开放/恢复） |
| `set-interval <分钟>` | 远程调整所有 agent 的同步间隔 |
| `machine bind <主机名> --user <名>` | 把一台机器绑定给某个员工 |
| `machine unbind <主机名>` | 解除绑定 |
| `machine list` | 查看所有机器的绑定情况 |
| `status` | 查看所有机器状态 |

---

## 3. OSS 文件结构

> **本节不再展开。OSS 的目录结构、每个对象的键与格式、两个 RAM 用户的权限，一律以 `OSS布局.md` 为准。**

要点摘要（细节见该文件对应章节）：

| 内容 | 在 `OSS布局.md` 的位置 |
|---|---|
| 两个平级顶级目录：`admin/` 与 `agent_workdir/` | §1、§2 |
| 机器身份用主机名、服务对象由绑定文件决定 | §3 |
| 各对象的键、生成函数、读写方 | §4 |
| `users.json` / `binding` / `status` / `policy` / `credentials.zip` 的字段 | §4.1 – §4.5 |
| 管理员与 agent 两套 RAM 权限 | §5 |
| `{员工}/data_collect/` 已实现（默认关闭） | §6 为权威说明，本文件 §13 仅存历史设计记录 |

### 3.1 一台机器的完整读写路径

以 DESKTOP-A 绑定给 work1 为例：

```
读  agent_workdir/policy.json                →  写入 HKLM 浏览器封禁（无条件）
读  agent_workdir/_bindings/DESKTOP-A.json   →  得知服务对象是 work1
读  agent_workdir/work1/credentials.zip      →  投递到 C:\Users\work1\
写  agent_workdir/_status/DESKTOP-A.json     →  上报本轮结果

机器未绑定时，只有第一步和最后一步执行——封禁照样生效。
```

---

## 4. 用户名单管理（功能详述）

### 4.1 名单是什么

名单登记"系统里有哪些员工在用 AI 工具"，存放在 OSS 的 `admin/users.json`：

```json
{
  "users": [
    { "windowsUser": "work1", "codexAccount": "ai-work1@company.com", "claudeAccount": "ai-work1@company.com", "enabled": true },
    { "windowsUser": "work2", "codexAccount": "ai-work2@company.com", "claudeAccount": "ai-work2@company.com", "enabled": false }
  ],
  "updatedAt": "2026-08-03T16:20:00Z"
}
```

| 字段 | 作用 |
|---|---|
| `windowsUser` | 云电脑上的 Windows 用户名。**决定 agent 去 OSS 的哪个目录取配置**，是整套系统的身份主键 |
| `codexAccount` / `claudeAccount` | 该员工使用的 AI 账户邮箱。**仅作备注**，程序不用它登录；作用是让管理员日后知道某人对应哪个账号，便于管理与续费 |
| `enabled` | 是否启用。停用后不再下发配置、不再纳入状态汇总 |

**为什么存 OSS 而不是管理员本地**：管理员可能换机器、可能不止一人。名单放 OSS，任何一台装了 `admin.exe` 并持有读写密钥的机器都能接手管理，不会因某台电脑损坏而丢失。

**为什么单独放在 `agent_workdir/` 外面**：agent 的 RAM 授权里根本没有这条路径（见 `OSS布局.md` §5）。即使某台云电脑的 agent 密钥泄露，攻击者也看不到公司有哪些员工、用了哪些 AI 账号。

### 4.2 名单的作用

名单不是可有可无的登记表，而是 **admin 端所有批量操作的依据**：

| 操作 | 依赖名单做什么 |
|---|---|
| `add-site` / `set-interval` 等策略下发 | 知道要写进**哪些**用户目录 |
| `status` 汇总 | 知道该去读**哪些**用户的状态；名单里有、但没有状态文件 → 判定该机器失联 |
| `login` | 校验用户是否已登记，避免把凭据上传到一个不存在的目录 |

没有名单，admin 就不知道系统里有几个人、该给谁下发、谁掉线了。

### 4.3 四个命令

#### `user add <名> [--codex 邮箱] [--claude 邮箱]`

把员工加入名单，状态置为启用。

```
admin.exe user add wangwu --codex ai-wangwu@company.com --claude ai-wangwu@company.com
```

- **幂等**：对已存在的用户再次执行会更新其邮箱备注，不会重复添加
- 此步**不产生任何凭据**，只是登记"这个人存在了"
- 邮箱参数可省略：`admin.exe user add wangwu` 也可以，只是名单里少了备注

#### `user list`

查看当前名单与启用状态。

```
admin.exe user list
```
```
USER       CODEX                    CLAUDE                   ENABLED
wangwu     ai-wangwu@company.com    ai-wangwu@company.com    true
lisi       ai-lisi@company.com      ai-lisi@company.com      false
```

#### `user disable <名>`

停用员工（离职、转岗、临时收回权限）。

```
admin.exe user disable wangwu
```

停用后：
- 后续策略下发**跳过**该用户
- `status` 把仍绑定该用户的机器标为 **DISABLED USER**（不是隐藏——一台还在运行却没人负责的机器必须留在视野里）
- agent 侧按计划**清除**该用户本地的凭据

> 停用是可逆的：`user add` 同名用户会重新置为启用。

#### `login --user <名> --tool <codex|claude|all>`

**代员工完成 OAuth 登录**，是名单登记之后的实际发凭据动作。详见 §4.3。

```
admin.exe login --user wangwu --tool all
```

管理员在浏览器里用该员工对应的 AI 账户完成授权（**密码由管理员保管，员工不知道**），token 在内存中组装成凭据文件后直接上传至 `wangwu/credentials.zip`。

- **必须先在名单里**，否则报错——这是防止凭据被传到错误目录的保护
- 一次只处理一个用户，每个工具都需要人工完成一次浏览器授权，**不支持批量**

### 4.4 完整生命周期示例

**新员工入职**

```
1. admin.exe user add wangwu --codex ai-wangwu@company.com --claude ai-wangwu@company.com
      ↓ 名单里有了这个人（此时还没有凭据）
2. admin.exe login --user wangwu --tool all
      ↓ 管理员浏览器完成两次授权，凭据上传 OSS
3. 用统一镜像开一台云电脑，创建 Windows 标准用户 wangwu
      ↓ 机器开机后 agent 用主机名上报自己，status 里显示 UNBOUND
4. admin.exe machine bind <主机名> --user wangwu
      ↓ agent 下次同步查到绑定，拉取 wangwu/ 下的配置
5. 员工登录 → 封禁策略已生效、AI 工具已是登录态，直接可用
```

**凭据过期**

```
admin.exe login --user wangwu --tool all     重新授权一次即可
      ↓ 各机器 agent 下次同步自动更新，员工无感
```

**员工离职**

```
admin.exe user disable wangwu
      ↓ 删除 OSS 上的 credentials.zip；agent 读到 404 后清除其本地凭据
```

---

## 5. 网站封禁策略（功能详述）

这是本方案的管控核心，单独展开。

### 5.1 封禁原理

**在浏览器的企业策略层封禁，不在网络层封域名。**

理由：ChatGPT/Claude 桌面应用与网页版走同一批域名，网络层封域名会**连桌面应用一起封掉**。浏览器策略是应用层的，只管浏览器，桌面应用不受影响——这是"只封网页、留应用"的关键。

### 5.2 三个浏览器的策略位置

| 浏览器 | 注册表路径（HKLM） | 值格式 |
|---|---|---|
| Chrome | `SOFTWARE\Policies\Google\Chrome\URLBlocklist` | 值名 `1`,`2`,`3`...，值为域名如 `openai.com` |
| Edge | `SOFTWARE\Policies\Microsoft\Edge\URLBlocklist` | 同 Chrome |
| Firefox | `SOFTWARE\Policies\Mozilla\Firefox\WebsiteFilter\Block` | 值为匹配模式，每个域名两条：`*://域名/*` 和 `*://*.域名/*` |

**为什么写 HKLM**：普通用户（标准用户）无权修改 HKLM，因此**无法自行解除封禁**。

**Chromium 与 Firefox 的差异**：Chrome/Edge 填裸域名即自动匹配全部子域和路径；Firefox 需要显式的通配模式，所以每个域名要展开成两条。

### 5.3 策略数据结构

字段定义见 `OSS布局.md` §4.4（策略是全局一份，不按员工分）。这里只说 agent 拿到之后怎么用：

- `blockEnabled` —— 总开关。`false` 时 agent **清除**所有封禁键（管理员临时开放场景）
- `blockedDomains` —— 域名清单，agent 据此重建注册表值
- `syncIntervalMinutes` —— **agent 的同步间隔**，管理员可远程调整（见 §5.6）

### 5.4 应用流程（agent 侧）

```
拉取 {用户名}/policy.json
   ↓
比对 ETag —— 与上次相同则跳过（避免无意义的注册表写入）
   ↓
blockEnabled == false ?
   ├─ 是 → 删除三个浏览器的策略键 → 记录"已解封"
   └─ 否 → 对每个浏览器：
            删除旧键 → 新建键 → 按序写入编号值
            （Chrome/Edge 用裸域名，Firefox 展开成两条模式）
   ↓
记录新 ETag 到本地标记文件
```

**为什么每次重建而不是增量修改**：策略清单是完整状态而非补丁，重建能保证本机与 OSS 严格一致，也自动处理域名被移除的情况。

### 5.5 下发流程（admin 侧）

```
管理员：admin.exe add-site gemini.google.com
   ↓
读取当前策略（从任一启用用户的目录）
   ↓
合并域名清单（去重、小写化、排序）
   ↓
写入 agent_workdir/policy.json（全局一份）
   ↓
各台 agent 下次同步（≤30 分钟）自动应用
   ↓
员工重启浏览器后生效
```

**为什么每个用户一份而不是共享一份**：OSS 权限按用户前缀隔离，agent 只能读自己目录。admin 端做批量写入，对管理员而言仍是"改一次"。

### 5.6 远程调整同步间隔

`syncIntervalMinutes` 让管理员**不重做镜像、不碰每台机器**就能改 agent 的同步频率。

**典型用途**：
- 排障时临时调到 5 分钟，改一次配置立刻看到效果
- 稳定后调回 30–60 分钟，减少 OSS 请求和日志量

**命令**：

```
admin.exe set-interval 5      # 全体改成 5 分钟
admin.exe set-interval 30     # 调回来
```

**agent 侧生效方式**：每次同步循环结束后读取本次拉到的 `syncIntervalMinutes`，作为下一次等待时长。所以改动**在下一个周期生效**——比如当前是 30 分钟，改成 5 分钟后，最多等 30 分钟切换过去。

**取值与兜底**：

| 情况 | agent 行为 |
|---|---|
| 字段缺失或为 0 | 用 agent 自带配置里的 `intervalMinutes` |
| 自带配置也没有 | 用内置默认 30 分钟 |
| 值 < 1 | 钳到 1 分钟 |
| 值 > 1440 | 钳到 1440 分钟（24 小时） |

**为什么要钳位**：防止误填 0 导致 agent 空转刷爆 OSS，或误填极大值导致机器长期失联、连"改回来"的指令都收不到。这是一个**能把自己锁死的配置项**，必须有下限保护。

### 5.7 生效条件与限制

| 项 | 说明 |
|---|---|
| **生效延迟** | agent 同步间隔（默认 30 分钟）+ 浏览器重启 |
| **浏览器重启** | 策略改变后必须重启浏览器，或在 `chrome://policy` 点"重新加载政策" |
| **覆盖范围** | 仅 Chrome/Edge/Firefox。其他浏览器（Brave/Opera/360 等）不覆盖 |
| **绿色版浏览器** | 不读系统策略，**封不住** —— 靠镜像里的 AppLocker 拦截运行 |
| **AppLocker 关系** | AppLocker 固化在镜像、不走 OSS。它负责挡住绿色版浏览器和注册表工具，是本策略的防绕过底座 |

> **三层防护缺一不可**：浏览器策略（封站）+ AppLocker（挡绿色版和 regedit）+ 标准用户（挡删 HKLM）。

---

## 6. AI 工具凭据分发（功能详述）

### 6.1 凭据从哪来

**管理员代员工登录**，员工全程不知道 AI 账户密码。

每个员工有**独立的** Codex / Claude 账户；`admin.exe login --user work1` 走完 OAuth 后，token 在**内存**中组装成凭据文件、打包上传 OSS，不落管理员机器的磁盘。

### 6.2 两个工具的登录差异

| | Codex | Claude |
|---|---|---|
| client_id | `app_EMoamEEZ73f0CkXaXp7hrann` | `9d1c250a-e61b-44d9-88ed-5944d1962f5e` |
| 授权端点 | `auth.openai.com/oauth/authorize` | `claude.ai/oauth/authorize` |
| token 端点 | `auth.openai.com/oauth/token` | `platform.claude.com/v1/oauth/token` |
| PKCE verifier | **64 字节 hex（128 字符）** | **32 字节 base64url（43 字符）** |
| token 请求体 | form-urlencoded | **JSON** |
| User-Agent | `codex-cli/0.91.0` | `axios/1.13.6`（用默认 UA 会被 WAF 限流） |
| 回调方式 | 跳转 `localhost:1455`，**程序自动接收** | 页面显示授权码，**管理员手动粘贴** |
| 授权 URL 特殊要求 | 带 `codex_cli_simplified_flow=true`、`id_token_add_organizations=true` | **第一个参数必须是 `code=true`** |

> 以上参数均已实测验证，实现时不得改动。

### 6.3 凭据文件格式

| 工具 | 文件 | 格式要点 |
|---|---|---|
| Codex | `~/.codex/auth.json` | 2 空格缩进、冒号后 1 空格、结尾换行、UTF-8 无 BOM。四个 token 字段（含 `account_id`）**必须非空**，否则 Codex 不认 |
| Claude | `~/.claude/.credentials.json` | **单行紧凑 JSON**，`expiresAt` / `refreshTokenExpiresAt` 是**毫秒**时间戳 |

**Codex 的 account_id 来源**：从 `id_token`（JWT）的 payload 里解出，claim 路径是 `https://api.openai.com/auth` → `chatgpt_account_id`。

**Claude 的订阅字段**：`subscriptionType` / `rateLimitTier` 服务端不返回，写空值即可，Claude Code 首次运行会自己补上。

### 6.4 分发流程（agent 侧）

```
拉取 {用户名}/credentials.zip
   ↓
比对 ETag —— 相同则跳过
   ↓
内存解压 → 得到若干凭据文件
   ↓
按映射表写入本机用户 profile：
   codex/auth.json          → C:\Users\{用户}\.codex\auth.json
   codex/config.toml        → C:\Users\{用户}\.codex\config.toml
   claude/.credentials.json → C:\Users\{用户}\.claude\.credentials.json
   claude.json              → C:\Users\{用户}\.claude.json
   ↓
修复 ACL，确保该用户可读
   ↓
结束 ChatGPT.exe / codex.exe / claude.exe 进程
   ↓
记录新 ETag
```

**为什么要杀进程**：这些工具**不热加载**凭据文件。Codex 源码明确写着"外部修改 auth.json 在显式 reload 前不会被感知"。不重启进程，写了也不生效。

**为什么不需要用户密码**：写文件到别人的 profile 目录、写 HKLM 注册表，只需要 SYSTEM 权限。agent 不模拟登录、不保存任何用户凭据。

**映射表是白名单**：只有表内的四个路径会被写入，未知条目直接跳过——同时防止了路径穿越。

### 6.5 凭据合并策略

`admin.exe login --tool claude` 只上传 Claude 凭据，但**不能覆盖掉已有的 Codex 凭据**。

因此 admin 端上传前先下载现有 zip、合并后再上传：

```
读取现有 agent_workdir/{用户}/credentials.zip（若存在）
   ↓
用新凭据覆盖同名条目，保留其他条目
   ↓
重新打包上传
```

---

## 7. 状态上报（功能详述）

### 7.1 上报内容

agent 每次同步后写自己那份状态文件，**按主机名而不是用户名**——机器还没绑定时也要能上报，否则管理员看不到它在等分配。

字段清单见 `OSS布局.md` §4.3。几个用来判断健康度的关键项：

- `policyEtag` / `credsEtag` —— 跨机器比对就能看出谁的配置落后
- `syncIntervalMinutes` —— 确认远程调整的间隔已被采纳
- `localUsers` / `boundUserExists` —— 发现意外账户、发现绑错了人
- `appLockerMode` —— `Enforce` / `Audit` / `None` / `Unknown`
- `errors` —— 本次同步中的错误列表

### 7.2 admin status 汇总

```
MACHINE     USER    LOCAL USERS          LAST SYNC  POLICY    BLOCK  APPLOCKER  STATE
DESKTOP-A   work1   Administrator,work1  2m ago     a1b2c3d4  on     Enforce    OK
DESKTOP-B   work2   Administrator        5m ago     a1b2c3d4  on     Enforce    USER MISSING
DESKTOP-C   -       Administrator,work3  1m ago     -         -      Audit      UNBOUND
DESKTOP-D   work4   -                    never      -         -      -          NO REPORT
```

一屏看出：
- **谁掉线**（LAST SYNC 过久 → STALE / NO REPORT）
- **谁还没分配**（UNBOUND —— 机器上报了但没绑定）
- **谁绑错了**（USER MISSING —— 绑定的员工在那台机器上没有 profile）
- **谁策略落后**（POLICY 的 ETag 与其他机器不一致）
- **谁 AppLocker 还没切强制**（Audit 说明还在审计模式，绿色版浏览器拦不住）
- **谁有错误**（STATE 显示错误数，下方列出具体错误）

### 7.3 容错原则

**单步失败不中断整个循环**：策略拉取失败不影响凭据分发；状态上报失败不影响本地已生效的配置。所有失败记录进 `errors` 数组，由 admin 端可见。

---

## 8. 同步触发机制（休眠唤醒问题）

### 8.1 问题

云电脑会休眠，而 Go 的 `time.Ticker` 基于**单调时钟**，休眠期间不计时：

```
14:00  同步一次，设定 30 分钟后再来
14:10  机器休眠          ← 定时器冻结
18:00  员工唤醒开始工作   ← 定时器从冻结点继续，还要再等 20 分钟
18:20  才同步            ← 期间 4 小时的策略变更和凭据更新全部滞后
```

**危害**：员工唤醒后立刻就在用，这段窗口里他拿的是过期策略（封禁清单可能已变）和可能已失效的凭据。

### 8.2 三重触发保障

单靠定时器不够，用三个互补的触发点覆盖各种情况：

| 触发点 | 覆盖场景 | 机制 |
|---|---|---|
| **① 服务启动即同步** | 开机、重启服务、agent 升级 | 服务启动后立即执行一次，不等第一个周期 |
| **② 电源事件唤醒** | 休眠/睡眠恢复 | 服务订阅 Windows 电源事件，收到"恢复"通知立即同步 |
| **③ 墙钟超期检查** | 电源事件丢失、时钟跳变、长时间挂起 | 每次循环用**绝对时间**比对上次同步时刻，超期立即同步 |

**为什么三个都要**：① 管重启，② 管唤醒，③ 是兜底——电源事件在某些虚拟化环境下可能收不到，此时靠绝对时间判断仍能自愈。

### 8.3 电源事件订阅

Windows 服务在声明接受的控制命令时加上 `SERVICE_ACCEPT_POWEREVENT`，即可收到电源通知：

| 事件 | 含义 | agent 动作 |
|---|---|---|
| `PBT_APMRESUMESUSPEND` | 从睡眠恢复 | 立即触发同步 |
| `PBT_APMRESUMEAUTOMATIC` | 系统自动唤醒 | 立即触发同步 |
| `PBT_APMSUSPEND` | 即将进入睡眠 | 记录时刻，不做同步 |

实现上，服务收到唤醒事件后向同步循环的 channel 发一个信号，循环立即执行而不等 ticker。

### 8.4 墙钟超期检查

除了单调时钟的 ticker，agent 额外维护一个**上次成功同步的墙钟时间**（持久化到本地状态文件）。

每轮循环开始时：

```
读取上次同步的墙钟时间
   ↓
当前时间 - 上次同步时间 > 同步间隔 ?
   ├─ 是 → 立即同步（说明期间休眠过或定时器不可靠）
   └─ 否 → 正常等待 ticker
```

**为什么持久化**：服务重启后内存状态丢失，从文件读取能判断"重启前多久同步过"，避免刚重启就重复同步，也避免长期停机后不同步。

**时钟跳变保护**：若计算出的间隔为负（系统时间被往回调），视为超期立即同步。

### 8.5 防抖

三个触发点可能在短时间内叠加（如开机同时触发①和③）。加一个最小间隔约束：

- 距上次同步**不足 30 秒**的触发被忽略
- 避免唤醒瞬间连续多次同步刷 OSS

---

## 9. 模块划分

```
go/
├── cmd/
│   ├── agent/          agent.exe 入口：命令分发 + 服务主循环
│   └── admin/          admin.exe 入口：命令分发
└── internal/
    ├── model/          数据结构（Policy / Status / Users / CredentialSet）
    ├── config/         配置文件加载与校验
    ├── ossclient/      OSS 读写封装（Get/Put/Head/List + ETag）
    ├── authflow/       OAuth 流程 + 凭据文件组装
    │   ├── pkce        两种 PKCE 编码
    │   ├── codex       Codex 授权/换 token/组装 auth.json
    │   └── claude      Claude 授权/换 token/组装 .credentials.json
    ├── policy/         网站封禁策略 → 注册表
    ├── creds/          凭据打包解包 + 投递 profile + 杀进程 + ACL
    ├── status/         状态采集与序列化
    ├── agentcore/      agent 同步逻辑（依赖注入，可测）
    ├── admincore/      admin 业务逻辑（名单、策略广播、凭据发布、状态汇总）
    └── winsvc/         Windows 服务封装 + 电源事件订阅
```

**职责边界**：
- `model` 只有数据结构，无逻辑
- `ossclient` 只管字节进出，不认识业务
- `authflow` 只负责"拿 token 并组装成文件内容"，不碰磁盘也不碰 OSS
- `policy` / `creds` 只管本机落地
- `agentcore` / `admincore` 负责编排，依赖接口而非具体实现（便于用假对象测试）
- `cmd/*` 只做命令解析与依赖装配

**跨平台策略**：Windows API 调用（注册表、服务、taskkill、icacls）放在 `_windows.go` 文件，配 `_stub.go` 占位，保证在 Linux 上能跑单元测试和 `go vet`。

---

## 10. 任务拆分

按依赖顺序，每个任务独立可测、可提交。

### 第一批：基础设施（无外部依赖）

| # | 任务 | 产出 | 测试要点 |
|---|---|---|---|
| 1 | 项目骨架与数据结构 | `go.mod`、`Makefile`、`model` 包 | JSON 序列化字段名与 OSS 对象一致 |
| 2 | PKCE 生成 | `authflow/pkce` | Codex 是 128 字符 hex、Claude 是 43 字符 base64url、两次调用不重复 |
| 3 | 配置加载 | `config` 包 | 缺字段报错、间隔默认 30 分钟 |
| 4 | OSS 客户端 | `ossclient` 包 | 用户目录 key 拼接、路径穿越防护 |

### 第二批：认证与凭据

| # | 任务 | 产出 | 测试要点 |
|---|---|---|---|
| 5 | Codex OAuth | `authflow/codex` | 授权 URL 参数齐全、从 id_token 解出 account_id、auth.json 格式（缩进/换行/非空校验） |
| 6 | Claude OAuth | `authflow/claude` | 授权 URL 以 `code=true` 开头、`code#state` 解析、credentials.json 单行且毫秒时间戳 |
| 7 | 凭据打包 | `creds/pack` | 打包解包往返一致、空集合报错、非 zip 报错 |

### 第三批：本机落地（Windows 相关）

| # | 任务 | 产出 | 测试要点 |
|---|---|---|---|
| 8 | **网站封禁策略** | `policy` 包 | Chromium 用裸域名、Firefox 每域名展开两条模式、空输入不panic |
| 9 | 凭据投递 | `creds/deploy` | 路径映射正确、未知条目跳过、写入内容一致 |
| 10 | 状态采集 | `status` 包 | 字段齐全、errors 序列化为 `[]` 而非 `null`、Age 计算 |

### 第四批：编排

| # | 任务 | 产出 | 测试要点 |
|---|---|---|---|
| 11 | agent 同步逻辑 | `agentcore` | ETag 相同时跳过应用、单步失败不中断、状态被上报、**间隔取值优先级（OSS > 本地配置 > 默认）**、**墙钟超期判断（含时钟回调）**、**30 秒防抖** |
| 12 | agent 主程序 | `cmd/agent` + `winsvc` | 用户名从 profile 路径提取、交叉编译通过、**服务启动即同步**、**电源事件订阅并触发同步** |
| 13 | admin 名单与策略 | `admincore` + 策略命令 | 加人幂等、停用用户不收策略、域名增删去重、**间隔钳位（<1 或 >1440 被修正）** |
| 14 | admin login | `cmd/admin/login` | 凭据合并不丢失其他工具、未在名单的用户报错 |
| 15 | admin status | `admincore/status` + 命令 | 标记 STALE / NO REPORT / UNBOUND / DISABLED USER |

### 第五批：交付

| # | 任务 | 产出 |
|---|---|---|
| 16 | 配置模板与构建 | `dist/` 下的配置样例、RAM 策略样例、部署说明；Makefile 的 dist 目标 |
| 17 | 清理与文档对齐 | 删除被取代的 PowerShell 脚本、更新 README 与操作手册 |

**保留的 PowerShell**：`01-Install-AITools.ps1`（装 AI 工具）和 `02-Manage-AIAccess.ps1`（配 AppLocker）只在**制作镜像时跑一次**，本质是调 Windows 原生命令，用 Go 包一层反而绕。

---

## 11. 关键约束（实现时不得改动）

这些是踩过坑验证出来的，写在这里防止实现时"优化"掉：

1. **PKCE 编码两者不同** —— Codex 用 hex，Claude 用 base64url。写错会导致 token 交换失败。
2. **User-Agent 必须伪装** —— Claude 用默认 UA 会被 WAF 限流（返回 `rate_limit_error`）。
3. **Claude 授权 URL 参数顺序** —— `code=true` 必须是第一个参数，不能用 `url.Values`（会按字母序排）。
4. **auth.json 格式严格** —— 2 空格缩进、结尾换行、四个 token 字段非空。格式不对 Codex 静默不认。
5. **credentials.json 是单行** —— 且时间戳是毫秒不是秒。
6. **写完凭据必须杀进程** —— 工具不热加载，不杀等于没写。
7. **策略写 HKLM 不是 HKCU** —— 写 HKCU 用户能自己删掉。
8. **凭据上传要合并** —— 单独更新一个工具不能覆盖掉另一个工具的凭据。
9. **同步间隔必须钳位** —— 下限 1 分钟、上限 1440 分钟。这是唯一能把机器锁到失联的配置项，没有下限保护会让误操作无法挽回。
10. **不能只依赖 time.Ticker** —— 它基于单调时钟，休眠期间不计时。必须叠加电源事件与墙钟超期检查，否则唤醒后员工会用着过期的策略和凭据。
11. **上次同步时间要持久化** —— 存本地文件而非仅内存，否则服务重启后无法判断已多久未同步。

---

## 12. 验收标准

**agent**：
- [ ] `agent.exe install` 后服务出现在服务列表且为自动启动
- [ ] `agent.exe sync` 能拉取策略并写入注册表（`chrome://policy` 可见）
- [ ] 员工浏览器访问被封域名显示"被组织策略阻止"，桌面应用正常
- [ ] 凭据投递后员工打开 Codex / Claude 无需登录
- [ ] OSS 上出现 `agent_workdir/_status/{主机名}.json`

**admin**：
- [ ] `admin.exe login --user X --tool all` 能完成两个工具的登录并上传
- [ ] `admin.exe add-site` 后所有启用用户的 policy.json 都更新
- [ ] `admin.exe disable-block` 后 agent 同步会清除封禁
- [ ] `admin.exe status` 正确显示所有机器，掉线的标 STALE
- [ ] `admin.exe set-interval 5` 后，各机器在下一个周期切换到 5 分钟，status 中的间隔字段随之更新

**休眠唤醒**：
- [ ] 机器休眠 1 小时后唤醒，agent 在 1 分钟内完成一次同步（不等满周期）
- [ ] 服务重启后立即同步一次，日志可见
- [ ] 连续触发（唤醒 + 重启）不会在 30 秒内重复同步

**端到端**：
- [ ] 新员工：加名单 → 代登录 → 开机器 → 员工登录即可用，零手动配置
- [ ] 改封禁：admin 改一次 → 30 分钟内所有机器生效
- [ ] 凭据过期：admin 重新登录 → 各机器自动更新，员工无感

---

## 13. `{员工}/data_collect/`（已实现，默认关闭）

采集**绑定员工**与 Codex / Claude 的交互数据，用于后续分析。**采集端已实现，默认关闭**——权威说明见 `OSS布局.md` §6（键结构、开关字段、权限边界、验证方法）与 `操作手册.md` §2.9（开关、前置条件、故障排查）。本节保留下面几个小节作为当初的设计记录，并标出哪些已经拍板、哪些仍然开放。

### 13.1 位置

**每个员工自己的目录下各一个**，不是一个共享的：

```
agent_workdir/
├── work1/
│   ├── credentials.zip
│   └── data_collect/       ← 这里
└── work2/
    └── data_collect/
```

这样一个员工的全部数据在一个前缀下：离职时删一个目录就干净，设生命周期规则也是一个前缀的事。理由详见 `OSS布局.md` §6。

### 13.2 落地前要定的几件事（已拍板 vs 仍开放）

| 问题 | 当初的说明 | 现状 |
|---|---|---|
| **数据从哪来** | Codex 与 Claude 的本地会话文件位置、格式、轮转规则 | **已决定**：`.claude/projects/**/*.jsonl` 与 `.codex/sessions/**/rollout-*.jsonl`，原文照传、不解析、不脱敏。见 `OSS布局.md` §6 |
| **按什么分区** | ~~员工维度已经由目录本身解决，剩下按日期分即可：`{员工}/data_collect/{日期}/`~~（未采用） | **已决定**：不按日期分区，OSS 键直接镜像源目录结构（`.claude/projects/...`、`.codex/sessions/rollout-*.jsonl`）。见 `OSS布局.md` §6 |
| **多久传一次** | 跟着现有同步周期走，还是独立节奏；会话文件写入中途上传会拿到半截 JSON | **已决定**：跟着同步周期走，不单独计时；靠去抖窗口（`collectQuietSeconds`，默认 60 秒）避开还在写的文件。见 `OSS布局.md` §6 |
| **重复上传** | 会话文件持续追加，同一文件会被反复上传 | **已决定**：本地 state 文件按 `(mtime, size)` 判断是否变化，不依赖内容哈希、不依赖读 OSS 反查。见 `OSS布局.md` §6 |
| **保留多久** | 交互数据体量远大于配置，需要 OSS 生命周期规则，否则成本会持续增长 | **仍待定**——生命周期规则要在 OSS 控制台单独配置，代码不涉及，落地时仍需要定 |

### 13.3 对现有权限的影响（重要，仍然有效）

Agent 的 RAM 授权（`dist/ram-policy-agent.json`）**默认仍然只有 `_status/` 的写权限**，不含 `data_collect/`。要启用采集，需要给它加一条：

```json
{
  "Effect": "Allow",
  "Action": ["oss:PutObject"],
  "Resource": ["acs:oss:*:*:your-bucket-name/agent_workdir/*/data_collect/*"]
}
```

**只给写，不给读。** Agent 没有理由读回采集数据——给了读权限，一台机器的密钥泄露就等于所有员工的对话记录泄露。这条比配置和凭据的敏感度高得多。代码侧也把这条约束做进了类型（`Putter` 只有 `Put`，见 `go/internal/agentcore/collect.go`）。

这条权限在功能**启用前不要提前加**；启用顺序、以及 `policy.json` 的 `collectEnabled` 开关，见 `OSS布局.md` §6 与 `操作手册.md` §2.9。

### 13.4 需要提前想清楚的合规问题

采集员工与 AI 的对话属于员工监控，落地前应当：

- 在员工手册或用工协议中明确告知采集范围与用途
- 明确谁能访问这些数据、访问要不要审批
- 确认是否涉及个人信息，是否需要单独同意

这部分不是技术问题，但会决定功能能不能上，建议在开发前先和客户的法务/HR 对齐。
