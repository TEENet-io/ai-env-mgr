# /etc/systemd/system/ai-env-mgr-admin.service.d/logdir.conf
# The console unit runs with ProtectSystem=strict; the unified-log directory
# passed as --log-dir must be writable or the process exits at start.
[Service]
ReadWritePaths=/var/log/ai-env-mgr
