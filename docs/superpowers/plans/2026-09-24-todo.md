# 待办总表(2026-09-24)

按"谁来做"和优先级分组。P1 = 流程走不通或有安全缺口;P2 = 明显改善;P3 = 可以等。
状态核对于 2026-09-24 16:45(北京时间)。

## A. 需要你操作的(不改代码)

| # | 事项 | 现状 / 做法 |
|---|---|---|
| A1 | 3 台机器迁到 1.3.0 + API 通道 | 全局目标已是 1.3.0。hv8uqpity23nkc7 已升到 1.3.0,但 14:22:56 注册时比你点"允许注册"早了 27 秒,被拒后一小时才重试,机器随后休眠,醒来后会自己注册上。lm0b1jcaty0wblw(09-21 起离线)、ufidwm5banmpszq(09-23 起离线)还在 1.2.14,开机后会自动升级。三台的注册窗口都到 **09-25 14:2x**;过期了就到机器页再点一次"允许注册" |
| A2 | 关掉两个 OSS 开关 | 总览"通道"列三台都变成 API 后,到 设置 › 设备通道 关掉"继续写 OSS 命令对象""继续从 OSS 导入状态" |
| A3 | 清理 OSS 旧对象 | A2 之后一周:`_bindings/`、`_status/`、`policy.json`、各员工 `credentials.zip`。清之前先确认没有 1.2.x 机器 |
| A4 | 取消卡住的 Actions 运行 | 第一次 v1.3.0 构建卡在上传那步(旧 workflow 没超时,最长 6 小时)。若还挂着,在 Actions 页 Cancel |
| A5 | 配告警渠道 | 设置 › 通知渠道(钉钉/飞书 webhook 或 SMTP)。不配的话告警只在告警页可见 |
| A6 | 决定是否打开令牌轮换 | 设置 › 告警与轮换,默认关 |
| A7 | 换一套 OSS 密钥 | 你说过之后整体重开。注意:1.2.x agent 内置旧 key,旧 key 要等全部机器到 1.3.x 才能停用;新 key 更新 `AGENT_OSS_*` secret 后重出 agent |
| A8 | 28 条历史审计标记 | 把从旧控制台导入的审计标成"已由旧控制台归档"的 SQL,之前被自动模式拦下,等你执行或同意 |
| A9 | codex-kiosk 旧 Actions 记录 | 需要 `gh auth login` 后删除 |
| A10 | 修订《ai工作间流程》 | ① "关闭 Codex 5 分钟"改为"控制台显示已收到新配置后重启";② 开户顺序:云侧绑定、员工首次登录放在"检查 Codex"之前;③ 补"客户端版本发布与策略变更"一节(发布页、封禁策略、告警、轮换,写明角色);④ 对账注明目前只有 AWS,时区统一 UTC;⑤ 关户保持机器绑定,实例释放后在机器页"注销" |

## B. 控制台开发

| # | 优先级 | 事项 | 说明 | 估算 |
|---|---|---|---|---|
| B1 | P1 | 模型上下架的"下发" | 流程 §3、§4 要求点"下发",控制台没有。员工的模型清单只在开户/改模型/重发时重生成:新模型到不了"全部模型"的员工,下架的模型留在选择器里。做:网关模型清单与上次下发的差异 → 一键给受影响员工重新生成配置 → 每台机器"已收到"状态;支持先发给一个测试员工(代替测试环境) | ✅ 已上线 321fb93(总览"模型下发";顺带修了 API 通道机器等的是令牌版本而非凭据包版本,d0ab01e) |
| B2 | P1 | 权限收紧 | 流程规定全局/高权限归平台管理员。改为 admin:封禁策略 `/sites/`、设置页写操作、发布页"设为全局/清除全局"、删除员工。运营保留开户、关户、绑定、额度、模型、下发、关闭 Codex | ✅ 已上线 a557418 |
| B3 | P3 | 控制台直连网关源站 | 员工页偶发"无法读取网关模型清单"。原因是本机到 Cloudflare 丢包;直连 `175.41.186.104` 并只对这个连接信任 Cloudflare Origin CA。详见 `数据库模式部署.md` 还没做的 | 半天 |
| B4 | P2 | 开户:邮箱、两侧绑定核对、开通邮件 | 员工加邮箱字段;用无影接口(已接)核对云电脑分配的无影用户与控制台绑定、用户名一致;两侧都好了自动发开通邮件(SMTP 已有) | 1 天 |
| B5 | P2 | 离职 7 天清理 | 关户满 7 天在告警页提示"待清理";无影接口确认实例已释放后自动注销机器 | ✅ 提醒已上线(告警"离职待清理",天数可设);自动注销暂不做,需要无影接口凭据 |
| B6 | P2 | 部署脚本 | 今天出过一次"迁移没权限、服务起不来 25 秒"。做一个 `deploy.sh`:先用属主账号 `ai-env-migrate up`,失败即停,再换程序、重启、健康检查,失败自动换回 `.prev` | ✅ 已上线 dd9670c(`dist/deploy/deploy-console.sh`) |

## C. Agent 1.3.1

前提:A1 完成(全部机器在 1.3.0 / API 通道)。

| # | 优先级 | 事项 | 说明 |
|---|---|---|---|
| C1 | — | ~~绑定变化时清掉前一个员工的本机凭据~~ | **不做**(2026-09-24 用户决定):机器换人要在阿里云新建用户,前一个员工的凭据在他自己的 Windows 用户目录里,新用户读不到;离职时令牌已在网关吊销;实例满 7 天连数据释放。员工换机器未离职时,要作废旧机器上的令牌就在员工页"重发令牌" |
| C2 | — | ~~独立更新器 + 启动失败自动恢复~~ | **不做**(2026-09-24 用户决定):新版 agent 先在测试环境验收,再"指定给机器"试一两台,最后设为全局。剩余风险是个别机器环境导致起不来,需人工上机 |
| C3 | P2 | 去掉内置 OSS key | API 通道下 agent 不再需要它:采集文件用控制台签发的一次性上传链接(`/agent/v1/collect/upload-url`),安装包用签发的下载链接,策略/凭据/状态/日志直连控制台。去掉后 agent.exe 里再没有能访问 OSS 的密钥。依赖 A2;之后令牌丢失的机器只能等管理员"允许注册"重新注册 |
| C4 | P2 | 注册被拒后的重试间隔 | 现在 1 小时,改成 5~10 分钟。今天 hv8 就是晚了 27 秒要等一小时 |

## D. 对账(独立程序 `billing-reconcile`)

| # | 优先级 | 事项 |
|---|---|---|
| D1 | P2 | AWS CUR 与 LiteLLM 按"模型 × 月"核对,差额 ≤ 0.1 USD 通过,超出的记录原因和处理状态(流程 §5) |
| D2 | P3 | GCP、Azure 账单接入 |
| D3 | P3 | 供应商折扣表与 5 天异议期跟踪(流程 §6) |

## E. 基础设施(可以等)

| # | 事项 | 说明 |
|---|---|---|
| E1 | RAM 实例角色 | 控制台现在用固定 AK/SK(600 环境文件);换 STS 需对着 ECS 元数据服务实测 |
| E2 | 数据库迁到 RDS | 现在是本机 Docker 里的 PostgreSQL 15,每晚备份到 OSS |
| E3 | KMS 主密钥 | 已决定暂不做 |

## 建议顺序

1. **本周**:A1~A2(跟着机器开机走)、B2、B6、B1。
2. **下周**:C4 可随时做;A2 完成后做 C3,一起出 agent 1.3.1;然后 A7 换密钥、A3 清 OSS。
3. **之后**:B4、B5、D1,最后 B3(用户定为低优先级),流程文档 A10 随 B1/B4/B5 落地一起改。
